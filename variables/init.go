package main

import "fmt"

// set values with initializer, can emit type definition
var i, j int = 1, 2

func main() {
	var c, python, java = true, false, "no!"
	fmt.Println(i, j, c, python, java)
}
